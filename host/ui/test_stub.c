#include "../test_assert.h"

/* Placeholder until Step 6 (panel→square / FSM). */
static void test_host_harness_runs(void)
{
    ASSERT_TRUE(1);
}

int main(void)
{
    test_host_harness_runs();
    return test_report();
}
